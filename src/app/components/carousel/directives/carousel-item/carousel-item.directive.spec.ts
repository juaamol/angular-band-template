import { CarouselItemDirective } from './carousel-item.directive';

describe('CarouselItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
